name: Test Homepage

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'index*.html'
      - 'styles.css'
      - 'src/browser/**'
      - 'cypress/e2e/homepage.cy.js'
      - 'cypress/e2e/accessibility.cy.js'
      - '.github/workflows/test-homepage.yml'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'index*.html'
      - 'styles.css'
      - 'src/browser/**'
      - 'cypress/e2e/homepage.cy.js'
      - 'cypress/e2e/accessibility.cy.js'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build for Homepage Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 # v2.19.1
        with:
          egress-policy: audit
      
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      
      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: '25'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build with Vite
        # The static-pages-emit plugin (scripts/vite-plugin-static-
        # pages.js) keeps the ~3 500 news/*.html, sitemap_*.html and
        # political-intelligence_*.html pages out of Rollup's module
        # graph; the build now needs <1 GB heap on the current corpus,
        # well under Node's default ~4 GB.
        run: npm run build
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: homepage-build
          path: dist/
          retention-days: 1 # Short retention for speed
  
  test-homepage:
    name: Homepage & Accessibility Tests
    runs-on: ubuntu-latest
    needs: build
    timeout-minutes: 5 # Max 5 minutes
    strategy:
      fail-fast: true # Stop immediately on failure
      matrix:
        spec:
          - 'cypress/e2e/homepage.cy.js'
          - 'cypress/e2e/accessibility.cy.js'
    
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@a5ad31d6a139d249332a2605b85202e8c0b78450 # v2.19.1
        with:
          egress-policy: audit
      
      - name: Checkout code
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      
      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: '25'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Download build artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: homepage-build
          path: dist/
      
      - name: Run Cypress tests
        uses: cypress-io/github-action@dace029018fcdf86e0df89a31bc3cfa5b32570d8 # v7.3.0
        with:
          start: npm run preview
          wait-on: 'http://localhost:4173'
          wait-on-timeout: 30 # Reduced from 60 for speed
          browser: chrome
          spec: ${{ matrix.spec }}
          config: video=false
          quiet: true # Faster output
      
      - name: Archive screenshots
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: homepage-screenshots-${{ strategy.job-index }}
          path: cypress/screenshots/
          retention-days: 7
  
  summary:
    name: Homepage Test Summary
    runs-on: ubuntu-latest
    needs: [build, test-homepage]
    if: always()
    
    steps:
      - name: Test Summary
        run: |
          echo "## 🏠 Homepage Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Build: ${{ needs.build.result }}" >> $GITHUB_STEP_SUMMARY
          echo "Tests: ${{ needs.test-homepage.result }}" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ needs.test-homepage.result }}" == "success" ]; then
            echo "### ✅ All homepage tests passed!" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ❌ Homepage tests failed" >> $GITHUB_STEP_SUMMARY
          fi
