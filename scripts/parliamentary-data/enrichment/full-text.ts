/**
 * @module parliamentary-data/enrichment/full-text
 * @description Re-export shim — original 417-line implementation was split
 * into `./full-text/{index,fetch-policy,stitcher,top-n-selection}.ts`.
 * Kept so historic imports (`enrichment/full-text.js`) keep resolving.
 */
export * from './full-text/index.js';
