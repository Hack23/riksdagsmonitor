/**
 * @module generate-news-indexes/helpers
 * @description Legacy compatibility shim — re-exports from `./helpers/index.js`.
 * Source split into focused modules per Hack23/riksdagsmonitor#2624 to keep
 * each file ≤ 300 lines (per `Secure_Development_Policy.md` audit budget).
 *
 * @author Hack23 AB
 * @license Apache-2.0
 */

export * from './helpers/index.js';
